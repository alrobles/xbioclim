% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioclim_window.R
\name{bioclim_window}
\alias{bioclim_window}
\title{Bioclimatic variables over an arbitrary window of months}
\usage{
bioclim_window(
  tas,
  tasmax,
  tasmin,
  pr,
  months = NULL,
  start = NULL,
  end = NULL,
  window = 3L,
  na.rm = FALSE,
  ...
)
}
\arguments{
\item{tas}{Numeric vector (length 12), matrix (pixels x 12),
\link[terra:SpatRaster-class]{SpatRaster} with 12 layers, or \link[=BioclimData-class]{BioclimData}.}

\item{tasmax}{Same form as \code{tas}: monthly maximum temperature.}

\item{tasmin}{Same form as \code{tas}: monthly minimum temperature.}

\item{pr}{Same form as \code{tas}: monthly precipitation.}

\item{months}{Integer vector of 1-based months in the window, e.g.
\code{c(6, 7)} for Jun-Jul or \code{c(12, 1, 2)} for DJF.  If \code{start} and
\code{end} are supplied, \code{months} is derived automatically.}

\item{start, end}{Optional character strings in \code{"MM-DD"} format.
When both are supplied, the months that overlap the day range are
selected.  Wrap-around across the year end is supported.}

\item{window}{Integer: length of the internal rolling sub-window used
to compute \code{bio08}..\code{bio19}.  Defaults to 3 (quarterly).  Must be
\verb{<= length(months)} for the rolling variables to be non-\code{NA}.}

\item{na.rm}{Logical. If \code{FALSE} (default), a single \code{NA} among the
selected months makes all outputs \code{NA} for that pixel. If \code{TRUE},
missing months are skipped.}

\item{...}{Not currently used.}
}
\value{
\itemize{
\item Vector input: a named numeric vector of length 19 (\code{bio01}..\code{bio19}).
\item Matrix / \link[=BioclimData-class]{BioclimData} input: a numeric matrix (pixels x 19).
\item \link[terra:SpatRaster-class]{SpatRaster} input: a 19-layer \link[terra:SpatRaster-class]{SpatRaster}.
}
}
\description{
Computes the 19 standard bioclimatic variables over a user-defined
subset of months.  This is useful when a species' relevant season
does not coincide with fixed quarters (e.g. eBird Status & Trends
breeding windows).
}
\details{
The base variables (\code{bio01}..\code{bio07}, \code{bio12}..\code{bio15}) are computed
directly over the selected months.  The rolling variables
(\code{bio08}..\code{bio11}, \code{bio16}..\code{bio19}) are computed over the best
contiguous \code{window}-month period that is fully contained in \code{months}.
If \code{length(months) < window} or no such contiguous period exists,
those columns are \code{NA}.
}
\seealso{
\code{\link[=bioclim_rolling]{bioclim_rolling()}} for rolling optimal windows over the full
year with a free \code{window} length.
}

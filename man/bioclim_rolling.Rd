% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioclim_window.R
\name{bioclim_rolling}
\alias{bioclim_rolling}
\title{Rolling optimal-window bioclimatic variables}
\usage{
bioclim_rolling(tas, tasmax, tasmin, pr, window = 3L, na.rm = FALSE, ...)
}
\arguments{
\item{tas}{Numeric vector (length 12), matrix (pixels x 12),
\link[terra:SpatRaster-class]{SpatRaster} with 12 layers, or \link[=BioclimData-class]{BioclimData}.}

\item{tasmax}{Same form as \code{tas}: monthly maximum temperature.}

\item{tasmin}{Same form as \code{tas}: monthly minimum temperature.}

\item{pr}{Same form as \code{tas}: monthly precipitation.}

\item{window}{Integer: length of the rolling window in months (2-11).
Default is 3.}

\item{na.rm}{Logical. If \code{FALSE} (default), a single \code{NA} makes all
outputs \code{NA} for that pixel. If \code{TRUE}, missing months are skipped.}

\item{...}{Not currently used.}
}
\value{
Named vector (length 19), matrix (pixels x 19), or 19-layer
\link[terra:SpatRaster-class]{SpatRaster}.
}
\description{
Computes the 19 bioclimatic variables using a rolling window of arbitrary
length over the full 12 months.  The base variables (\code{bio01}..\code{bio07},
\code{bio12}..\code{bio15}) are annual, while the rolling variables
(\code{bio08}..\code{bio11}, \code{bio16}..\code{bio19}) use the best \code{window}-month period.
With \code{window = 3} this is equivalent to the standard BIO08-BIO19.
}

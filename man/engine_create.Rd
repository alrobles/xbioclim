% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{engine_create}
\alias{engine_create}
\alias{engine_open}
\alias{engine_set_output}
\alias{engine_set_mask}
\alias{engine_set_threads}
\alias{engine_set_tile_size}
\alias{engine_compute}
\title{BioclimEngine XPtr interface}
\usage{
engine_create()

engine_open(xptr, tas_files, tasmax_files, tasmin_files, pr_files)

engine_set_output(xptr, path)

engine_set_mask(xptr, mask_path)

engine_set_threads(xptr, n)

engine_set_tile_size(xptr, tile_size)

engine_compute(xptr)
}
\arguments{
\item{xptr}{External pointer returned by \code{engine_create()}.}

\item{tas_files}{Character vector (length 1 or 12): mean temperature files.}

\item{tasmax_files}{Character vector (length 1 or 12): maximum temperature files.}

\item{tasmin_files}{Character vector (length 1 or 12): minimum temperature files.}

\item{pr_files}{Character vector (length 1 or 12): precipitation files.}

\item{path}{Character scalar: output GeoTIFF file path.}

\item{mask_path}{Character scalar: mask raster path, or \code{""} for none.}

\item{n}{Integer scalar: number of OpenMP threads (values < 1 are clamped to 1).}

\item{tile_size}{Integer scalar: tile width and height in pixels (default 256).}
}
\value{
\code{engine_create()} returns an \code{externalptr} to a new
\code{BioclimEngine} object.  \code{engine_compute()} returns the output
file path as a character scalar.  All other functions return \code{NULL}
invisibly.
}
\description{
Low-level XPtr wrappers for the \code{BioclimEngine} C++ class.  These
functions expose the tiled bioclimatic-variable computation pipeline to R
via opaque external pointers.

\code{engine_create()} allocates a new engine.
\code{engine_open()} sets the monthly climate input file paths.
\code{engine_set_output()} sets the output GeoTIFF path.
\code{engine_set_mask()} sets an optional mask raster.
\code{engine_set_threads()} sets the number of OpenMP threads.
\code{engine_set_tile_size()} overrides the tile dimensions.
\code{engine_compute()} runs the full tiled pipeline (requires GDAL).
}
\seealso{
\code{\link{has_gdal}}, \code{\link{gdal_can_open}}
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarterly.R
\name{quarterly_variables}
\alias{quarterly_variables}
\alias{quarterly_variables.default}
\title{Quarterly / seasonal climate variables}
\usage{
quarterly_variables(tas, tasmax, tasmin, pr, months, na.rm = FALSE, ...)

\method{quarterly_variables}{default}(tas, tasmax, tasmin, pr, months, na.rm = FALSE, ...)
}
\arguments{
\item{tas}{Numeric vector (length 12), matrix (pixels x 12),
\link[terra:SpatRaster-class]{SpatRaster} with 12 layers, or \link[=BioclimData-class]{BioclimData}.}

\item{tasmax}{Same form as \code{tas}: monthly maximum temperature.}

\item{tasmin}{Same form as \code{tas}: monthly minimum temperature.}

\item{pr}{Same form as \code{tas}: monthly precipitation.}

\item{months}{Integer vector of 1-based month indices to include (e.g.
\code{c(1, 2, 3)} for a fixed quarter, or \code{c(12, 1, 2)} for DJF).}

\item{na.rm}{Logical. If \code{FALSE} (default), a single \code{NA} among the
selected months makes all outputs \code{NA} for that pixel. If \code{TRUE},
missing months are skipped.}

\item{...}{Not currently used.}
}
\value{
\itemize{
\item Vector input: a named numeric vector of length 6.
\item Matrix / \link[=BioclimData-class]{BioclimData} input: a numeric matrix (pixels x 6) with
columns \code{tmean_s}, \code{tmax_max}, \code{tmin_min}, \code{trange}, \code{pr_tot}, \code{pr_cv}.
\item \link[terra:SpatRaster-class]{SpatRaster} input: a 6-layer \link[terra:SpatRaster-class]{SpatRaster} with the same
geometry and the same names.
}
}
\description{
Computes six quarterly/seasonal variables for an arbitrary set of months.
}
\details{
The output variables are:
\itemize{
\item \code{tmean_s} — mean \code{tas} over the selected months.
\item \code{tmax_max} — maximum monthly \code{tasmax} among the selected months.
\item \code{tmin_min} — minimum monthly \code{tasmin} among the selected months.
\item \code{trange} — \code{tmax_max - tmin_min}.
\item \code{pr_tot} — total \code{pr} over the selected months.
\item \code{pr_cv} — coefficient of variation of monthly \code{pr}
(\code{100 * sd_pop / mean}), matching the BIO15 scaling.
}

Use \code{\link[=quarterly_fixed]{quarterly_fixed()}} for the four standard fixed quarters and
\code{\link[=quarterly_rolling]{quarterly_rolling()}} for rolling 3-month windows.
}
